package lars.rosenkilde.musicplayer;

/*
import android.media.MediaDataSource;
import android.media.MediaMetadataRetriever;
import android.net.Uri;
import android.provider.MediaStore;

import java.io.File;
import java.io.IOException;
import java.net.URL;

import javax.sql.DataSource;

public class MetaDataExtractor {
    File song = new File("android.resource://lars.rosenkilde.musicplayer/raw/dummysong.mp3");
    DataSource ds = MediaStore.Audio.Media anager.createDataSource(song.)
    MediaMetadataRetriever mmr = new MediaMetadataRetriever().setDataSource;

    String albumName = mmr.extractMetadata(MediaMetadataRetriever.METADATA_KEY_ALBUM);
    String albumArtist = mmr.extractMetadata(MediaMetadataRetriever.METADATA_KEY_ARTIST);
}

*/